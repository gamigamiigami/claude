現在の日本時間（Asia/Tokyo）を取得し、以下のルールで挨拶を返す。

- 4時〜11時（4:00以上11:00未満）：「おはよう」
- 11時〜16時（11:00以上16:00未満）：「こんにちは」
- 16時〜翌4時（16:00以上または4:00未満）：「こんばんは」

挨拶の一言だけを返すこと。それ以外の説明や文章は一切不要。

現在時刻の取得には `date` コマンドを使うこと：
```
TZ=Asia/Tokyo date +%H
```
